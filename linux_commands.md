Linux file system -> / -> root
                    /bin -> binaries(ls,cd,cat)
                    /boot -> kernel & boot loader
                    /dev -> devices()
                    /etc -> configurations
                    /home -> user files
                    /lib -> libraries
                    /media -> exteranal sytsem
                    /mnt
                    /opt
                    /root
                    /run
                    /sbin -> system binaries
                    /srv
                    /proc -> kernel & process info
                    /usr -> user programs
                    /sys
                    /tmp  -> temporary files
                    /var -> logs & caches

File System and Commands:->
list    ls ->
        ls -l ->
        ls -a ->
        ls -la ->

cd -> immiidiate previous back directory -> cd -
       cd .. -> immidiate parrent
       mkdir -> make new directory
       touch file.txt -> create a file
       rm file.txt -> delete a file
       cat file.txt -> see the inside
       echo hello world >> file.txt -> for input
       head/tails -n 2 file.txt
       for totally new content echo hello world > file.txt

For folder delete->
       rm -r folder
       rm -rr folder 
       rm -rf folder

 cp file.txt file1.txt  -> copy file.txt to file1.txt
 cp -r folder1 folder2 -> copy folder1 to folder2
        mv file.txt file1.txt -> rename or move file.txt to file1.txt
        pwd -> present working directory
        find . -name "file.txt" -> find file in current directory
        find / -name "file.txt" -> find file in whole system
        locate file.txt -> find file quickly using database
       



        

                    

